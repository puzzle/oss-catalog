apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oss-catalog.fullname" . }}-test-connection"
  labels:
    {{- include "oss-catalog.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oss-catalog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
